<UserControl x:Class="ZzaDashboard.View.CustomerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:data="clr-namespace:Zza.Data;assembly=Zza.Data"
             xmlns:local="clr-namespace:ZzaDashboard.View"
             xmlns:viewmodel="clr-namespace:ZzaDashboard.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type data:Customer}">
            <Border BorderThickness="3"
                    BorderBrush="Blue">
                <StackPanel>
                    <TextBlock Text="{Binding FirstName}" />
                    <TextBlock Text="{Binding LastName}" />
                    <TextBlock Text="{Binding Phone}" />
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>
    
    <UserControl.DataContext>
        <viewmodel:CustomerListViewModel />
    </UserControl.DataContext>
    
    <Grid>
        <ListBox 
            ItemsSource="{Binding Customers}" 
            SelectedItem="{Binding SelectedCustomer}"
            />
    </Grid>
</UserControl>
