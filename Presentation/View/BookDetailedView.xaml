<UserControl
    x:Class="Presentation.View.BookDetailedView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Presentation.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding SelectedBook}"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <Grid>
        <StackPanel Margin="2">
            <Label
                Grid.Row="0"
                Grid.Column="0"
                Content="Title" />
            <TextBox
                Grid.Row="0"
                Grid.Column="1"
                Text="{Binding Title}" />

            <Label
                Grid.Row="1"
                Grid.Column="0"
                Content="Author" />
            <TextBox
                Grid.Row="1"
                Grid.Column="1"
                Text="{Binding Author}" />

            <Label
                Grid.Row="2"
                Grid.Column="0"
                Content="Year" />
            <TextBox
                Grid.Row="2"
                Grid.Column="1"
                Text="{Binding Year}" />
        </StackPanel>
    </Grid>
</UserControl>